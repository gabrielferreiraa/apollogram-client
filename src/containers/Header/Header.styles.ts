import styled from "styled-components";
import { NavbarHeading } from "@blueprintjs/core";

export const User = styled(NavbarHeading)`
  display: flex;
  align-items: center;

  img {
    margin-right: 0.5em;
  }
`;
